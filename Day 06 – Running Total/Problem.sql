-- Table: Sales
-- Columns: id, amount

-- Write a query to calculate running total of sales.

CREATE TABLE Sales (
    id INT,
    amount INT
);

INSERT INTO Sales VALUES
(1, 100),
(2, 200),
(3, 300);